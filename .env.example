# Copiar este archivo a ".env" y completar. El ".env" real NUNCA se sube a
# ningun repositorio ni se publica: solo vive en el servidor donde corre el bot.

# --- Los 3 datos que faltan del checklist de Meta ---------------------------
META_WHATSAPP_TOKEN=
META_PHONE_NUMBER_ID=

# Inventar una palabra clave cualquiera (ej: "territorio2026") y pegarla tal
# cual en el panel de Meta cuando pida el "Verify Token" del webhook.
META_VERIFY_TOKEN=

# --- Mismos datos que ya usa el formulario de inteligencia/index.html -------
# La clave es la PUBLICABLE (sb_publishable_...), nunca la secreta.
SUPABASE_URL=https://nvjmgpmvhrodirykoirq.supabase.co
SUPABASE_CLAVE_PUBLICA=sb_publishable_kgLRqlc49ebATAS4DWt7Xg_IBtzSoZr

# --- Opcional ---------------------------------------------------------------
PORT=3000
